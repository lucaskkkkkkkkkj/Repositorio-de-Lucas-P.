1.
#include <stdio.h>

int main(){
    int v, i;
    int vetor[10];
    for(i=0; i<10; i++){
        printf("Digite o %d termo do vetor: ", i+1);
        scanf("%d", &vetor[i]);
    }
    printf("A soma dos termos do vetor eh: ");
    v = 0;
    for(i=0; i<10; i++){
        v = v + vetor[i];
    }
    printf("%d\n", v);
    return 0;

}

2.
#include <stdio.h>

int main(){
    int i, maior, menor;
    int vetor[15];
    for(i=0; i<15; i++){
        printf("Digite o %dº termo do vetor: ", i+1);
        scanf("%d", &vetor[i]);
    }
    printf("O maior numero do vetor é: ");
    maior = vetor[0];
    for(i=1; i<15; i++){
        if(vetor[i]>maior){
            maior = vetor[i];
        }
    }
    printf("%d\n", maior);
    printf("O menor numero do vetor é: ");
    menor = vetor[0];
    for(i=1; i<15; i++){
        if(vetor[i]<menor){
            menor = vetor[i];
        }
    }
    printf("%d\n", menor);

    return 0;

}

3. 
#include <stdio.h>

int main(){
    int i;
    float media;
    int vetor[8];
    for(i=0; i<8; i++){
        printf("Digite o %dº termo do vetor: ", i+1);
        scanf("%d", &vetor[i]);

    }
    media = 0;
    for(i=0; i<8; i++){
        media = media + vetor[i];
    }
    media /= 8;
    printf("A média dos valores do vetor é: %f1", media);
    return 0;

}

4.
#include <stdio.h>

int main(){
    int i, par, impar;
    int vetor[20];
    for (i=0; i<20; i++){
        printf("Digite o %dº termo do vetor: ", i+1);
        scanf("%d", &vetor[i]);
    }
    par = 0;
    impar = 0;
    for (i=0; i<20; i++){
        if (vetor[i] % 2 == 0){
            par++;
        } else {
            impar++;
        }
    }
    printf("O vetor possui %d números pares e %d números ímpares.\n", par, impar);
    return 0;
}

5.
#include <stdio.h>

int main(){
    int i, busca;
    int vetor[10];
    for (i=0; i<10; i++){
        printf("Digite o %dº termo do vetor: ", i+1);
        scanf("%d", &vetor[i]);
    }
    
    printf("Qual número você quer buscar no vetor? ");
    scanf("%d", &busca);
    for (i=0; i<10; i++){
        if (vetor[i] == busca){
            printf("o numero %d esta na posiçao %d do vetor", busca, i+1);
            return 0;
        } else {
            printf("o numero %d nao esta no vetor", busca);
            return 0;
        }
    }
    return 0;
}

6.
#include <stdio.h>

int main(){
    int i, numero;
    printf("Qual numero quer o fatorial?");
    scanf("%d", &numero);
    int vetor[numero];
    vetor[0] = 1;
    for(i=1; i<numero; i++){
        vetor[i] = vetor[i-1] * (i+1);
    }
    printf("O fatorial de %d é %d\n", numero, vetor[numero-1]); 
    return 0;
}

7.
#include <stdio.h>

int main(){
    int i;
    int vetor[10];
    for(i=0; i<10; i++){
        printf("Digite o %dº termo do vetor: ", i+1);
        scanf("%d", &vetor[i]);
    }
    printf("Ordem inversa:\n");
    for(i=9; i>=0; i--){
        printf("%d ", vetor[i]);
    }
    return 0;
}

8. 
#include <stdio.h>

int main(){
    int i, range, fibonacci;
    printf("Quantos termos da sequência de Fibonacci você quer ver? ");
    scanf("%d", &range);
    int vetor[range];
    for (i = 0; i < range; i++) {
        if (i == 0) {
            fibonacci = 0;
        } else if (i == 1) {
            fibonacci = 1;
        } else {
            fibonacci = vetor[i - 1] + vetor[i - 2];
        }
        vetor[i] = fibonacci;
        printf("%d ", vetor[i]);
    }
    return 0;
}

9.
#include <stdio.h>

int main() {
    int vetor1[5], vetor2[5], vetor3[10];
    int i, j = 0;

    
    printf("Digite os 5 elementos do vetor 1:\n");
    for(i = 0; i < 5; i++) {
        scanf("%d", &vetor1[i]);
    }

    
    printf("\nDigite os 5 elementos do vetor 2:\n");
    for(i = 0; i < 5; i++) {
        scanf("%d", &vetor2[i]);
    }

    
    for(i = 0; i < 5; i++) {
        vetor3[j++] = vetor1[i];
        vetor3[j++] = vetor2[i];
    }

    
    printf("\nVetor intercalado: ");
    for(i = 0; i < 10; i++) {
        printf("%d ", vetor3[i]);
    }

    return 0;
}

10.
#include <stdio.h>

int main() {
    int vetorA[5], vetorB[5];
    int i, produto_escalar = 0;

    // Leitura do vetorA
    printf("Digite os 5 elementos do vetor A:\n");
    for(i = 0; i < 5; i++) {
        scanf("%d", &vetorA[i]);
    }

    // Leitura do vetorB
    printf("\nDigite os 5 elementos do vetor B:\n");
    for(i = 0; i < 5; i++) {
        scanf("%d", &vetorB[i]);
    }

    // Cálculo do produto escalar
    for(i = 0; i < 5; i++) {
        produto_escalar += vetorA[i] * vetorB[i];
    }

    printf("\nProduto escalar = %d\n", produto_escalar);

    return 0;
}